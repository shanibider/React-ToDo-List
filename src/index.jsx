import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// For locall run: 'npm install', to install the node modules and 'npm run dev', to launch the react project in the browser

ReactDOM.render(<App />, document.getElementById("root"));